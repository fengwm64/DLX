services:
  deeplx:
    image: ghcr.io/fengwm64/deeplx:latest
    restart: always
    ports:
      - "1188:1188"
    environment:
      - PROXY=${PROXY:?set PROXY in .env}
      - RESIN_ACCOUNT=${RESIN_ACCOUNT:-dlx}
      - RESIN_PLATFORM_ID=${RESIN_PLATFORM_ID:-7d9e1c7b-00a2-47f6-a858-d59a1111269b}
      - RESIN_STICKY_PREFIX=${RESIN_STICKY_PREFIX:-DLX}
      - RESIN_PROXY_HOST=${RESIN_PROXY_HOST:-resin:2260}
      - RESIN_ADMIN_TOKEN=${RESIN_ADMIN_TOKEN:?set RESIN_ADMIN_TOKEN in .env}
      # - TOKEN=helloworld
      # - DL_SESSION=xxxxxx
    networks:
      - default
      - resin_default

networks:
  resin_default:
    external: true
